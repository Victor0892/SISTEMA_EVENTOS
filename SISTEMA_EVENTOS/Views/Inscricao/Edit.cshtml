@model SISTEMA_EVENTOS.MODEL.Models.Inscricao

@{
    ViewData["Title"] = "Edit";
}

<h1>Editar Inscrição</h1>

<h4>Inscricao</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="Id" type="hidden" />
            <div class="form-group">
                <label asp-for="Id" class="control-label"></label>
                <input asp-for="Id" class="form-control" />
                <span asp-validation-for="Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ParticipanteId" class="control-label"></label>
                <input asp-for="ParticipanteId" class="form-control" />
                <span asp-validation-for="ParticipanteId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EventoId" class="control-label"></label>
                <input asp-for="EventoId" class="form-control" />
                <span asp-validation-for="EventoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Salvar Dados" class="btn btn-primary" style="margin-top: 20px;" />
            </div>
        </form>
    </div>
</div>

<div class="alert-info">
    @{
        if (ViewData["Mensagem"] != null)
        {
            @ViewData["Mensagem"]
            ;
        }
    }
</div>
<div class="alert-danger">
    @{
        if (ViewData["MensagemErro"] != null)
        {
            @ViewData["MensagemErro"]
            ;
        }
    }
</div>

<div style="margin-top: 20px;">
    <a class="btn btn-secondary" asp-action="Index">Voltar para a lista</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
